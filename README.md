Day2 of 100days:
today i built a contact manager app:
functionalities:
    add contact, view contact, delete contact

Day3 of 100days:
adding localStorage to my todo app
localStorage functions:
    setItems("key","value")
    getItems()
    clear()
    JSON.Stringfy()
    JSON.Parse()

added search, but failed to add functionality
added edit, but didn't added functionality
added completed button but didn't added functionality